/** MÉTODOS */
lmht {
    linhas-superiores: 2;
    altura: 30px;
    largura: herdar;
    colunas: 2;
    opacidade: 0.5;
    indice-z: 5;
    transformar: translação(15graus);
    deslocamento: raio(conter 200graus);
    filtro-fundo: borrar(10px);
}

lmht {
    colunas: 20px;
    opacidade: 80%;
}

lmht {
    espacamento: calcular(100px - 80px);
    imagem-máscara: gradiente-linear(90graus, azul, vermelho);
    origem-imagem-borda: gradiente-linear(90deg, verde, amarelo);
    filtro: borrar(80%); 
}

lmht {
    filtro: brilho(0.4); 
}

lmht {
    filtro: contraste(10px); 
    filtro-fundo: projetar-sombra(0.5rem 0.5rem);
}

lmht {
    trajeto-deslocamento: raio(lado-mais-próximo 45deg);
    deslocamento: raio(conter 45deg);
    filtro-fundo: projetar-sombra(15px 15px 15px azul);
}

lmht {
    transformar: escalamento-3d(1, 1, 1);
    variacao-fonte-alternativa: variar-caractere(10);
}

lmht {
    transformar: escalamento-3d(1.3, 1.3, 1.3);
    variacao-fonte-alternativa: espirrar(1);
}

lmht {
    transformar: escalamento-3d(1.8, 1, 0.5);
    variacao-fonte-alternativa: estilístico(1);
}

lmht {
    conteúdo: contador(contador1, romano-minusculo);
    variacao-fonte-alternativa: conjunto-estilos(1, 2, 6);
}

lmht {
    variacao-fonte-alternativa: ornamentos(10);
}

lmht {
    variacao-fonte-alternativa: anotação(1);
}